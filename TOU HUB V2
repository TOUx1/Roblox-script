-- // TOU HUB v34 ULTIMATE FULL 100% - CITY THAILAND 2 + อ้อโต้ขายบ้าน 🏠💰🔥🇹🇭🌈🎵⚙️🚪🚀👻
-- คัดลอกทั้งหมด → รัน → LeftControl → ครบทุกอย่าง + ฟาร์มไม่หยุดแม้ Kick!

local NothingLibrary = loadstring(game:HttpGetAsync('https://raw.githubusercontent.com/3345-c-a-t-s-u-s/NOTHING/main/source.lua'))()
local Notify = NothingLibrary.Notification()

-- ตัวแปรควบคุม
local NotifyEnabled = true
local AutoRejoinEnabled = true
local AutoResumeFarmEnabled = true

-- สถานะฟาร์ม
local FarmStates = {
	Sell = false,
	Box = false,
	Medicine = false
}

-- Global Reference
_G.SellToggle = nil
_G.BoxToggle = nil
_G.MedToggle = nil
_G.FarmLoop = nil

local function SafeNotify(data)
	if NotifyEnabled then
		Notify.new(data)
	end
end

SafeNotify({
	Title = "🔥 TOU HUB v34 LOADED เต็มระบบ!",
	Description = "ขายบ้านรัว + ฟาร์มไม่หยุด + ครบทุกปุ่ม!",
	Duration = 8,
	Icon = "rbxassetid://8997385628"
})

local Windows = NothingLibrary.new({
	Title = "TOU HUB v34",
	Description = "City Thailand 2 — ฟาร์มไม่หยุดแม้ตาย",
	Keybind = Enum.KeyCode.LeftControl,
	Logo = 'http://www.roblox.com/asset/?id=18898582662'
})

-- ====================== Character Safe ======================
local function GetHumanoid()
	local player = game.Players.LocalPlayer
	local char = player.Character or player.CharacterAdded:Wait()
	return char:WaitForChild("Humanoid", 10)
end

local function GetRootPart()
	local player = game.Players.LocalPlayer
	local char = player.Character or player.CharacterAdded:Wait()
	return char:WaitForChild("HumanoidRootPart", 10)
end

-- ====================== Auto Rejoin + Resume ======================
local TeleportService = game:GetService("TeleportService")
local PlaceId = game.PlaceId

local function RejoinAndResume()
	if not AutoRejoinEnabled then return end
	SafeNotify({Title="🔄 ถูก Kick!", Description="Rejoin + รันฟาร์มใน 5 วินาที...", Duration=5})
	task.wait(5)
	TeleportService:Teleport(PlaceId, game.Players.LocalPlayer)
end

game:GetService("GuiService").ErrorMessageChanged:Connect(RejoinAndResume)
TeleportService.TeleportInitFailed:Connect(RejoinAndResume)
game.Players.LocalPlayer.OnTeleport:Connect(function(state)
	if state == Enum.TeleportState.Failed then RejoinAndResume() end
end)

game.Players.LocalPlayer.CharacterAdded:Connect(function()
	if AutoResumeFarmEnabled then
		task.wait(3)
		if FarmStates.Sell and _G.SellToggle then _G.SellToggle:Callback(true) end
		if FarmStates.Box and _G.BoxToggle then _G.BoxToggle:Callback(true) end
		if FarmStates.Medicine and _G.MedToggle then _G.MedToggle:Callback(true) end
		SafeNotify({Title="💰 กลับมาแล้ว!", Description="ฟาร์มรันต่ออัตโนมัติ!", Duration=3})
	end
end)

-- ====================== Farm Tab + อ้อโต้ขายบ้าน ======================
local FarmTab = Windows:NewTab({Title = "💰 Farm", Description = "ฟาร์ม + ขายบ้านรัว", Icon = "rbxassetid://7733960981"})

local FarmLeft = FarmTab:NewSection({Title = "💸 อ้อโต้ฟามเงิน", Icon = "rbxassetid://7743869054", Position = "Left"})
local AutoHouseSection = FarmTab:NewSection({Title = "🏠 อ้อโต้ขายบ้าน", Icon = "rbxassetid://7733993369", Position = "Right"})
local FarmRight = FarmTab:NewSection({Title = "⚙️ การตั้งค่า", Icon = "rbxassetid://7733993369", Position = "Right"})
local FarmInfo = FarmTab:NewSection({Title = "ℹ️ Info", Icon = "rbxassetid://7733964719", Position = "Right"})

local URL_FARM = "https://raw.githubusercontent.com/TOUx1/Roblox-script/refs/heads/main/Sell"
local URL_BOX  = "https://pastefy.app/VaHDLWjU/raw"
local URL_MED  = "https://pastefy.app/ji38FSCI/raw"

local function SilentLoad(url)
	task.spawn(function()
		pcall(function()
			loadstring(game:HttpGet(url))()
		end)
	end)
end

-- ปุ่มอ้อโต้ขายบ้าน (เพิ่มมาแล้ว!)
AutoHouseSection:NewButton({
	Title = "🏠 อ้อโต้ขายบ้าน",
	Description = "กด = ขายบ้านรัว ๆ รวยทะลัก!",
	Callback = function()
		SafeNotify({Title="🏠 ขายบ้านเริ่มแล้ว!", Description="เงินไหลมาเทมา!", Duration=5})
		pcall(function()
			loadstring(game:HttpGet("https://pastefy.app/8V7h6QG3/raw"))()
		end)
	end
})

-- Toggle Sell
_G.SellToggle = FarmLeft:NewToggle({
	Title = "⚡ Auto farm(ซื้อบ้านก่อน)(ปิง)",
	Default = false,
	Callback = function(state)
		FarmStates.Sell = state
		if state then
			SafeNotify({Title="🚀 FARM ON", Description="Sell ทุก 0.05s", Duration=2})
			SilentLoad(URL_FARM)
			_G.FarmLoop = game:GetService("RunService").Heartbeat:Connect(function()
				if not FarmStates.Sell then return end
				SilentLoad(URL_FARM)
				task.wait(0.05)
			end)
		else
			if _G.FarmLoop then _G.FarmLoop:Disconnect() _G.FarmLoop = nil end
			SafeNotify({Title="🛑 FARM OFF", Description="หยุด!", Duration=2})
		end
	end
})

-- Toggle Box
_G.BoxToggle = FarmLeft:NewToggle({
	Title = "📦 อ้อโต้ส่งกล่อง (ช้า)",
	Default = false,
	Callback = function(state)
		FarmStates.Box = state
		if state then
			SafeNotify({Title="📦 BOX ON", Description="ทุก 15s", Duration=2})
			SilentLoad(URL_BOX)
			task.spawn(function()
				while FarmStates.Box do
					task.wait(15)
					if not FarmStates.Box then break end
					SilentLoad(URL_BOX)
				end
			end)
		else
			SafeNotify({Title="🛑 BOX OFF", Description="หยุด!", Duration=2})
		end
	end
})

-- Toggle Medicine
_G.MedToggle = FarmLeft:NewToggle({
	Title = "💊 อ้อโต้จ่ายยา(แล็ค)(ช้า)",
	Default = false,
	Callback = function(state)
		FarmStates.Medicine = state
		if state then
			SafeNotify({Title="💊 MEDICINE ON", Description="ทุก 1s", Duration=2})
			SilentLoad(URL_MED)
			task.spawn(function()
				while FarmStates.Medicine do
					task.wait(1)
					if not FarmStates.Medicine then break end
					SilentLoad(URL_MED)
				end
			end)
		else
			SafeNotify({Title="🛑 MEDICINE OFF", Description="หยุด!", Duration=2})
		end
	end
})

-- Slider + Noclip + Info
FarmRight:NewSlider({Title = "🏃 WalkSpeed", Min = 16, Max = 300, Default = 16, Callback = function(v) local hum = GetHumanoid() if hum then hum.WalkSpeed = v end end})
FarmRight:NewSlider({Title = "🦘 JumpPower", Min = 50, Max = 500, Default = 50, Callback = function(v) local hum = GetHumanoid() if hum then hum.JumpPower = v end end})
FarmRight:NewKeybind({Title = "🔥 Noclip (Hold)", Default = Enum.KeyCode.N, Callback = function()
	local noclip = false
	local conn
	conn = game:GetService("RunService").Stepped:Connect(function()
		if not noclip then conn:Disconnect() return end
		local root = GetRootPart()
		if root and root.Parent then
			for _, part in pairs(root.Parent:GetDescendants()) do
				if part:IsA("BasePart") and part ~= root then
					part.CanCollide = false
				end
			end
		end
	end)
	noclip = not noclip
end})

FarmInfo:NewTitle("👑 TOU HUB v34 🇹🇭")
FarmInfo:NewButton({Title = "📱 Discord (แท้)", Callback = function() setclipboard("https://discord.gg/cj93WrHEaf") SafeNotify({Title="คัดลอกแล้ว!"}) end})

-- ====================== วาป 11 จุด ======================
local WarpTab = Windows:NewTab({Title = "🌌 วาปไปทีต่างๆ", Description = "11 จุดลับ!", Icon = "rbxassetid://7743866277"})
local WarpQuick = WarpTab:NewSection({Title = "🚀 วาปด่วน", Position = "Left"})

local function TeleportTo(pos, desc)
	local root = GetRootPart()
	if root then
		local old = {}
		for _, p in pairs(root.Parent:GetDescendants()) do
			if p:IsA("BasePart") and p ~= root then
				old[p] = p.CanCollide
				p.CanCollide = false
			end
		end
		task.wait(0.1)
		root.CFrame = CFrame.new(pos)
		task.wait(0.4)
		for p, c in pairs(old) do if p and p.Parent then p.CanCollide = c end end
		SafeNotify({Title="🚀 วาปแล้ว!", Description=desc, Duration=1.5})
	end
end

local warps = {
	{"👮‍♂️ สถานีตำรวจ", Vector3.new(173.66, 177.31, 248.56), "สถานีตำรวจ"},
	{"🏠 บ้าน", Vector3.new(251.54, 167.46, 104.53), "บ้าน"},
	{"🔫 บ้านซื้อปืนM4", Vector3.new(383.23, 169.29, 294.55), "M4"},
	{"⛩️ วัด", Vector3.new(434.08, 173.84, -47.66), "วัด"},
	{"🚗 ขายรถ", Vector3.new(369.82, 171.46, -202.83), "ขายรถ"},
	{"🍚 ร้านข้าว", Vector3.new(-30.64, 178.42, 223.76), "ร้านข้าว"},
	{"🏫 โรงเรียน", Vector3.new(-116.03, 165.30, 160.62), "โรงเรียน"},
	{"🏥 โรงบาล", Vector3.new(-164.13, 177.18, 216.73), "โรงบาล"},
	{"🚀 ปล่อยจรวด", Vector3.new(-326.59, 263.38, 230.80), "จรวด"},
	{"🛒 เซเว่น", Vector3.new(356.42, 168.22, 42.65), "เซเว่น"},
	{"🔫 ปืนใต้ดิน", Vector3.new(-75.99, 163.69, 220.78), "ปืนใต้ดิน"}
}

for _, w in ipairs(warps) do
	WarpQuick:NewButton({Title = w[1], Callback = function() TeleportTo(w[2], w[3]) end})
end

-- ====================== ลำโพงดิสโก้ ======================
local SpeakerTab = Windows:NewTab({Title = "🎵 ปรับสีลำโพง", Description = "Loop + ทดสอบสี", Icon = "rbxassetid://7733993369"})
local SpeakerLeft = SpeakerTab:NewSection({Title = "🎨 เลือกสี Loop", Position = "Left"})
local SpeakerRight = SpeakerTab:NewSection({Title = "⚙️ ควบคุม Loop", Position = "Right"})

local Colors = {
	{ name = "สีฟ้า", id = 1013 },
	{ name = "สีแดง", id = 331 },
	{ name = "สีเขียว", id = 1020 },
	{ name = "สีชมพูเค็ม", id = 1016 },
	{ name = "สีเหลือง", id = 1009 },
	{ name = "สีส้ม", id = 106 },
	{ name = "สีม่วง", id = 1015 },
	{ name = "สีชมพูอ่อน", id = 330 },
	{ name = "สีขาว", id = 1001 },
	{ name = "สีเทา", id = 302 },
	{ name = "สีดำ", id = 1003 }
}

local SelectedColors = {1, 2}
local LoopSpeed = 0.5
local IsColorLooping = false
local ColorLoop = nil

SpeakerLeft:NewDropdown({Title = "🎨 สีที่ 1", Data = {"สีฟ้า","สีแดง","สีเขียว","สีชมพูเค็ม","สีเหลือง","สีส้ม","สีม่วง","สีชมพูอ่อน","สีขาว","สีเทา","สีดำ"}, Default = "สีฟ้า", Callback = function(selected) for i,c in ipairs(Colors) do if c.name == selected then SelectedColors[1] = i break end end end})
SpeakerLeft:NewDropdown({Title = "🎨 สีที่ 2", Data = {"สีฟ้า","สีแดง","สีเขียว","สีชมพูเค็ม","สีเหลือง","สีส้ม","สีม่วง","สีชมพูอ่อน","สีขาว","สีเทา","สีดำ"}, Default = "สีแดง", Callback = function(selected) for i,c in ipairs(Colors) do if c.name == selected then SelectedColors[2] = i break end end end})

SpeakerRight:NewSlider({Title = "⚡ ความเร็ว Loop", Min = 0.1, Max = 5, Default = 0.5, Callback = function(speed) LoopSpeed = speed end})

SpeakerRight:NewToggle({Title = "▶️ เริ่ม/หยุด Loop สี", Default = false, Callback = function(state)
	if state then
		IsColorLooping = true
		SafeNotify({Title="🌈 LOOP ON", Description="ลำโพงดิสโก้!", Duration=2})
		ColorLoop = task.spawn(function()
			while IsColorLooping do
				pcall(function() workspace:WaitForChild("DRadio_Script"):WaitForChild("Color"):FireServer(BrickColor.new(Colors[SelectedColors[1]].id)) end)
				task.wait(LoopSpeed)
				pcall(function() workspace:WaitForChild("DRadio_Script"):WaitForChild("Color"):FireServer(BrickColor.new(Colors[SelectedColors[2]].id)) end)
				task.wait(LoopSpeed)
			end
		end)
	else
		IsColorLooping = false
		if ColorLoop then task.cancel(ColorLoop) end
		SafeNotify({Title="⏹️ LOOP OFF", Duration=2})
	end
end})

SpeakerRight:NewButton({Title = "🧪 ทดสอบสีที่ 1", Callback = function()
	pcall(function() workspace:WaitForChild("DRadio_Script"):WaitForChild("Color"):FireServer(BrickColor.new(Colors[SelectedColors[1]].id)) end)
end})

SpeakerRight:NewButton({Title = "🧪 ทดสอบสีที่ 2", Callback = function()
	pcall(function() workspace:WaitForChild("DRadio_Script"):WaitForChild("Color"):FireServer(BrickColor.new(Colors[SelectedColors[2]].id)) end)
end})

-- ====================== การตั้งค่า ======================
local SettingsTab = Windows:NewTab({Title = "⚙️ การตั้งค่า", Description = "Rejoin + Resume + Notify", Icon = "rbxassetid://7733993369"})
local SettingsSec = SettingsTab:NewSection({Title = "🔔 การแจ้งเตือน", Position = "Left"})
local ServerSec = SettingsTab:NewSection({Title = "🚪 เซิร์ฟเวอร์", Position = "Right"})

SettingsSec:NewToggle({Title = "🔔 เปิด/ปิดการแจ้งเตือน", Default = true, Callback = function(state) NotifyEnabled = state end})
SettingsSec:NewToggle({Title = "🔄 Auto Rejoin ถ้า Kick", Default = true, Callback = function(state) AutoRejoinEnabled = state end})
SettingsSec:NewToggle({Title = "💰 Auto Resume ฟาร์มหลังกลับมา", Default = true, Callback = function(state) AutoResumeFarmEnabled = state end})

ServerSec:NewButton({Title = "🚪 ย้ายไปเซิร์ฟใหม่", Callback = function() TeleportService:Teleport(PlaceId, game.Players.LocalPlayer) end})
ServerSec:NewButton({Title = "🔄 ย้าย + รีโหลด", Callback = function() TeleportService:TeleportToPlaceInstance(PlaceId, game.JobId, game.Players.LocalPlayer) end})

loadstring(game:HttpGet("https://raw.githubusercontent.com/TOUxIllIll0O00OO/RobloxscriptTOU/refs/heads/main/Report%20a%20message"))()
