--[[
    🔥🔥 wino444's REMOTE INTERCEPTOR v2.1 - PART 1/2 🔥🔥
    💀 GUI Framework, Tab System, & Basic Setup 💀
    📝 Copy this part first!
]]

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- 📊 Global Variables
local interceptedRemotes = {}
local currentTab = nil
local tabs = {
    {name = "📡 INTERCEPTED", content = nil},
    {name = "🔧 EXECUTE", content = nil},
    {name = "📊 LOGS", content = nil}
}

-- 🎨 Create Main GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "RemoteInterceptor"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- 🖼️ Main Frame
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 800, 0, 500)
mainFrame.Position = UDim2.new(0.5, -400, 0.5, -250)
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

-- 🎭 Corner Radius
local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainFrame

-- 🟣 Gradient Background
local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 30, 40)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 20, 25))
}
gradient.Rotation = 45
gradient.Parent = mainFrame

-- 🏷️ Title Bar
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 40)
titleBar.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
titleBar.BorderSizePixel = 0
titleBar.Parent = mainFrame

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 12)
titleCorner.Parent = titleBar

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -80, 1, 0)
titleLabel.Position = UDim2.new(0, 10, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "💀 wino444's REMOTE INTERCEPTOR 💀"
titleLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
titleLabel.TextScaled = true
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = titleBar

-- 🔴 Close Button
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 30, 0, 30)
closeButton.Position = UDim2.new(1, -40, 0, 5)
closeButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
closeButton.Text = "✕"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.TextScaled = true
closeButton.Font = Enum.Font.GothamBold
closeButton.BorderSizePixel = 0
closeButton.Parent = titleBar

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 15)
closeCorner.Parent = closeButton

-- 📁 Tab System Container
local tabContainer = Instance.new("Frame")
tabContainer.Size = UDim2.new(0, 150, 1, -40)
tabContainer.Position = UDim2.new(0, 0, 0, 40)
tabContainer.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
tabContainer.BorderSizePixel = 0
tabContainer.Parent = mainFrame

local tabCorner = Instance.new("UICorner")
tabCorner.CornerRadius = UDim.new(0, 12)
tabCorner.Parent = tabContainer

-- 📋 Content Area
local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, -150, 1, -40)
contentFrame.Position = UDim2.new(0, 150, 0, 40)
contentFrame.BackgroundTransparency = 1
contentFrame.Parent = mainFrame

-- 🟢 Tab Button Creator
local function createTabButton(tabInfo, index)
    local tabButton = Instance.new("TextButton")
    tabButton.Size = UDim2.new(1, -10, 0, 50)
    tabButton.Position = UDim2.new(0, 5, 0, (index-1) * 55 + 10)
    tabButton.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
    tabButton.Text = tabInfo.name
    tabButton.TextColor3 = Color3.fromRGB(200, 200, 200)
    tabButton.TextScaled = true
    tabButton.Font = Enum.Font.Gotham
    tabButton.BorderSizePixel = 0
    tabButton.Parent = tabContainer
    
    local tabCorner = Instance.new("UICorner")
    tabCorner.CornerRadius = UDim.new(0, 8)
    tabCorner.Parent = tabButton
    
    -- Hover Effect
    tabButton.MouseEnter:Connect(function()
        if currentTab ~= index then
            TweenService:Create(tabButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(50, 50, 55)}):Play()
        end
    end)
    
    tabButton.MouseLeave:Connect(function()
        if currentTab ~= index then
            TweenService:Create(tabButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(35, 35, 40)}):Play()
        end
    end)
    
    tabButton.MouseButton1Click:Connect(function()
        switchTab(index)
    end)
    
    return tabButton
end

-- 📄 Intercepted Tab Creator
local function createInterceptedTab()
    local scrollFrame = Instance.new("ScrollingFrame")
    scrollFrame.Name = "ScrollFrame"
    scrollFrame.Size = UDim2.new(1, -20, 1, -100)
    scrollFrame.Position = UDim2.new(0, 10, 0, 10)
    scrollFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    scrollFrame.BorderSizePixel = 0
    scrollFrame.ScrollBarThickness = 8
    scrollFrame.ScrollBarImageColor3 = Color3.fromRGB(100, 100, 100)
    scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
    scrollFrame.Parent = contentFrame
    
    local scrollCorner = Instance.new("UICorner")
    scrollCorner.CornerRadius = UDim.new(0, 8)
    scrollCorner.Parent = scrollFrame
    
    local header = Instance.new("TextLabel")
    header.Size = UDim2.new(1, 0, 0, 30)
    header.BackgroundTransparency = 1
    header.Text = "📡 INTERCEPTED REMOTES"
    header.TextColor3 = Color3.fromRGB(255, 255, 255)
    header.TextScaled = true
    header.Font = Enum.Font.GothamBold
    header.Parent = scrollFrame
    
    local buttonsFrame = Instance.new("Frame")
    buttonsFrame.Name = "ButtonsFrame"
    buttonsFrame.Size = UDim2.new(1, 0, 1, -30)
    buttonsFrame.Position = UDim2.new(0, 0, 0, 30)
    buttonsFrame.BackgroundTransparency = 1
    buttonsFrame.Parent = scrollFrame
    
    return scrollFrame, buttonsFrame
end

-- 🔧 Execute Tab Creator
local function createExecuteTab()
    local executeFrame = Instance.new("Frame")
    executeFrame.Size = UDim2.new(1, -20, 1, -20)
    executeFrame.Position = UDim2.new(0, 10, 0, 10)
    executeFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    executeFrame.BorderSizePixel = 0
    executeFrame.Parent = contentFrame
    
    local executeCorner = Instance.new("UICorner")
    executeCorner.CornerRadius = UDim.new(0, 8)
    executeCorner.Parent = executeFrame
    
    -- Code Editor
    local codeBox = Instance.new("TextBox")
    codeBox.Name = "TextBox"
    codeBox.Size = UDim2.new(1, 0, 0, 200)
    codeBox.Position = UDim2.new(0, 0, 0, 10)
    codeBox.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    codeBox.BorderSizePixel = 0
    codeBox.Text = "-- Paste intercepted remote code here\n-- Click 'RUN' to execute"
    codeBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    codeBox.TextXAlignment = Enum.TextXAlignment.Left
    codeBox.TextYAlignment = Enum.TextYAlignment.Top
    codeBox.MultiLine = true
    codeBox.TextWrapped = true
    codeBox.Font = Enum.Font.Code
    codeBox.TextSize = 14
    codeBox.ClearTextOnFocus = false
    codeBox.Parent = executeFrame
    
    local codeCorner = Instance.new("UICorner")
    codeCorner.CornerRadius = UDim.new(0, 8)
    codeCorner.Parent = codeBox
    
    -- Run Button
    local runButton = Instance.new("TextButton")
    runButton.Size = UDim2.new(0, 100, 0, 40)
    runButton.Position = UDim2.new(0, 10, 1, -50)
    runButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    runButton.Text = "▶️ RUN"
    runButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    runButton.TextScaled = true
    runButton.Font = Enum.Font.GothamBold
    runButton.BorderSizePixel = 0
    runButton.Parent = executeFrame
    
    local runCorner = Instance.new("UICorner")
    runCorner.CornerRadius = UDim.new(0, 8)
    runCorner.Parent = runButton
    
    -- Copy Button
    local copyButton = Instance.new("TextButton")
    copyButton.Size = UDim2.new(0, 100, 0, 40)
    copyButton.Position = UDim2.new(0, 120, 1, -50)
    copyButton.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    copyButton.Text = "📋 COPY"
    copyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    copyButton.TextScaled = true
    copyButton.Font = Enum.Font.GothamBold
    copyButton.BorderSizePixel = 0
    copyButton.Parent = executeFrame
    
    local copyCorner = Instance.new("UICorner")
    copyCorner.CornerRadius = UDim.new(0, 8)
    copyCorner.Parent = copyButton
    
    -- Status Label
    local statusLabel = Instance.new("TextLabel")
    statusLabel.Size = UDim2.new(1, 0, 0, 30)
    statusLabel.Position = UDim2.new(0, 0, 0, 220)
    statusLabel.BackgroundTransparency = 1
    statusLabel.Text = "Status: Ready"
    statusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    statusLabel.TextScaled = true
    statusLabel.Font = Enum.Font.Gotham
    statusLabel.Parent = executeFrame
    
    -- Button Functions
    runButton.MouseButton1Click:Connect(function()
        local code = codeBox.Text
        if code and code ~= "" and not string.find(code, "-- Paste intercepted remote code here") then
            statusLabel.Text = "Status: Executing..."
            statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
            
            local success, error = pcall(function()
                loadstring(code)()
            end)
            
            if success then
                statusLabel.Text = "Status: Executed successfully! ✅"
                statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
            else
                statusLabel.Text = "Status: Error - " .. tostring(error)
                statusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
            end
        else
            statusLabel.Text = "Status: No code to execute!"
            statusLabel.TextColor3 = Color3.fromRGB(255, 150, 0)
        end
    end)
    
    copyButton.MouseButton1Click:Connect(function()
        local code = codeBox.Text
        if code and code ~= "" then
            setclipboard(code)
            statusLabel.Text = "Status: Code copied to clipboard! 📋"
            statusLabel.TextColor3 = Color3.fromRGB(0, 200, 255)
            
            TweenService:Create(copyButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 200, 100)}):Play()
            wait(0.2)
            TweenService:Create(copyButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 150, 255)}):Play()
        end
    end)
    
    return executeFrame
end

-- 📊 Logs Tab Creator
local function createLogsTab()
    local logsFrame = Instance.new("ScrollingFrame")
    logsFrame.Size = UDim2.new(1, -20, 1, -20)
    logsFrame.Position = UDim2.new(0, 10, 0, 10)
    logsFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    logsFrame.BorderSizePixel = 0
    logsFrame.ScrollBarThickness = 8
    logsFrame.ScrollBarImageColor3 = Color3.fromRGB(100, 100, 100)
    logsFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
    logsFrame.Parent = contentFrame
    
    local logsCorner = Instance.new("UICorner")
    logsCorner.CornerRadius = UDim.new(0, 8)
    logsCorner.Parent = logsFrame
    
    local logsLabel = Instance.new("TextLabel")
    logsLabel.Size = UDim2.new(1, 0, 0, 30)
    logsLabel.BackgroundTransparency = 1
    logsLabel.Text = "📊 INTERCEPTION LOGS"
    logsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    logsLabel.TextScaled = true
    logsLabel.Font = Enum.Font.GothamBold
    logsLabel.Parent = logsFrame
    
    return logsFrame
end

-- 🔄 Tab Switching Function
function switchTab(tabIndex)
    -- Hide all content
    for _, tabContent in pairs(tabs) do
        if tabContent.content then
            tabContent.content.Visible = false
        end
    end
    
    -- Update tab buttons
    for i, tab in ipairs(tabs) do
        local tabButton = tabContainer:FindFirstChild("TabButton" .. i)
        if tabButton then
            if i == tabIndex then
                TweenService:Create(tabButton, TweenInfo.new(0.2), {
                    BackgroundColor3 = Color3.fromRGB(255, 50, 50),
                    TextColor3 = Color3.fromRGB(255, 255, 255)
                }):Play()
            else
                TweenService:Create(tabButton, TweenInfo.new(0.2), {
                    BackgroundColor3 = Color3.fromRGB(35, 35, 40),
                    TextColor3 = Color3.fromRGB(200, 200, 200)
                }):Play()
            end
        end
    end
    
    -- Show selected content
    if tabs[tabIndex].content then
        tabs[tabIndex].content.Visible = true
        currentTab = tabIndex
    end
end

-- 🎛️ Initialize UI Tabs
for i, tabInfo in ipairs(tabs) do
    local tabButton = createTabButton(tabInfo, i)
    tabButton.Name = "TabButton" .. i
    
    if i == 1 then
        tabs[i].content, _ = createInterceptedTab()
    elseif i == 2 then
        tabs[i].content = createExecuteTab()
    elseif i == 3 then
        tabs[i].content = createLogsTab()
    end
    
    if tabs[i].content then
        tabs[i].content.Name = "TabContent" .. i
        tabs[i].content.Visible = (i == 1)
    end
end

-- Set default tab
switchTab(1)

-- 🔴 Close functionality
closeButton.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

-- 🎭 Opening Animation
mainFrame.Size = UDim2.new(0, 0, 0, 0)
TweenService:Create(mainFrame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 800, 0, 500)
}):Play()

print("💀 wino444's Remote Interceptor - PART 1/2 Loaded! 💀")
print("📝 GUI Framework initialized. Waiting for PART 2...")
--[[
    🔥🔥 wino444's REMOTE INTERCEPTOR v2.1 - PART 2/2 🔥🔥
    💀 Remote Hook System, Interception Engine & Debug 💀
    📝 Paste this AFTER PART 1!
]]

-- 🕸️ Remote Interception System
local function interceptRemotes()
    print("🔥 Interceptor Started! Checking for Remotes... 🔥")
    
    -- Hook RemoteEvent.FireServer
    local oldFireServer = {}
    local function hookRemoteEvent(remote)
        if oldFireServer[remote] then return end
        
        print("📡 Hooked RemoteEvent: " .. remote.Name)
        oldFireServer[remote] = remote.FireServer
        remote.FireServer = function(self, ...)
            local args = {...}
            
            print(string.format("🚨 Intercepted RemoteEvent: %s with %d args", remote.Name, #args))
            
            table.insert(interceptedRemotes, {
                name = remote.Name,
                args = args,
                timestamp = tick(),
                remote = remote,
                type = "RemoteEvent"
            })
            
            -- Update UI
            local interceptedTab = tabs[1].content:FindFirstChild("ScrollFrame")
            local buttonsFrame = interceptedTab and interceptedTab:FindFirstChild("ButtonsFrame")
            if interceptedTab and buttonsFrame then
                local remoteButton = Instance.new("TextButton")
                local timestamp = os.date("%H:%M:%S", math.floor(tick()))
                remoteButton.Size = UDim2.new(1, -10, 0, 60)
                remoteButton.Position = UDim2.new(0, 5, 0, #interceptedRemotes * 65)
                remoteButton.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
                remoteButton.Text = string.format("[%s] %s (%d args) [Event]", timestamp, remote.Name, #args)
                remoteButton.TextColor3 = Color3.fromRGB(255, 255, 255)
                remoteButton.TextScaled = true
                remoteButton.Font = Enum.Font.Gotham
                remoteButton.BorderSizePixel = 0
                remoteButton.Parent = buttonsFrame
                
                local buttonCorner = Instance.new("UICorner")
                buttonCorner.CornerRadius = UDim.new(0, 8)
                buttonCorner.Parent = remoteButton
                
                -- Generate Lua code
                local code = string.format("-- Intercepted: %s (RemoteEvent)\n-- Timestamp: %s\n-- Args: %d\n\n", 
                    remote.Name, timestamp, #args)
                
                for i, arg in ipairs(args) do
                    if typeof(arg) == "string" then
                        code = code .. string.format('local arg%d = "%s"\n', i, tostring(arg):gsub('"', '\\"'))
                    elseif typeof(arg) == "number" then
                        code = code .. string.format('local arg%d = %s\n', i, tostring(arg))
                    elseif typeof(arg) == "table" then
                        local success, json = pcall(function()
                            return game:GetService("HttpService"):JSONEncode(arg)
                        end)
                        code = code .. string.format('local arg%d = %s\n', i, success and json or "table")
                    else
                        code = code .. string.format('local arg%d = %s\n', i, tostring(arg))
                    end
                end
                
                code = code .. string.format('\n-- Execute with:\ngame:GetService("ReplicatedStorage"):WaitForChild("%s"):FireServer(%s)\n', 
                    remote.Name, table.concat({"arg1", "arg2", "arg3", "arg4", "arg5", "arg6", "arg7", "arg8"}, ", "))
                
                -- Click to load code
                remoteButton.MouseButton1Click:Connect(function()
                    switchTab(2)
                    local executeTab = tabs[2].content
                    local codeBox = executeTab:FindFirstChild("TextBox")
                    if codeBox then
                        codeBox.Text = code
                    end
                end)
                
                -- Update scroll size
                interceptedTab.CanvasSize = UDim2.new(0, 0, 0, #interceptedRemotes * 65 + 50)
            end
            
            -- Update logs
            local logsFrame = tabs[3].content
            if logsFrame then
                local logEntry = Instance.new("TextLabel")
                logEntry.Size = UDim2.new(1, -10, 0, 25)
                logEntry.Position = UDim2.new(0, 5, 0, logsFrame.AbsoluteCanvasSize.Y)
                logEntry.BackgroundTransparency = 1
                logEntry.Text = string.format("[%s] Intercepted %s with %d args [Event]", 
                    os.date("%H:%M:%S"), remote.Name, #args)
                logEntry.TextColor3 = Color3.fromRGB(0, 255, 0)
                logEntry.TextXAlignment = Enum.TextXAlignment.Left
                logEntry.TextSize = 12
                logEntry.Font = Enum.Font.Code
                logEntry.Parent = logsFrame
                
                logsFrame.CanvasSize = UDim2.new(0, 0, 0, logsFrame.AbsoluteCanvasSize.Y + 30)
            end
            
            -- Call original function
            return oldFireServer[remote](self, ...)
        end
    end
    
    -- Hook RemoteFunction.InvokeServer
    local oldInvokeServer = {}
    local function hookRemoteFunction(remote)
        if oldInvokeServer[remote] then return end
        
        print("🔧 Hooked RemoteFunction: " .. remote.Name)
        oldInvokeServer[remote] = remote.InvokeServer
        remote.InvokeServer = function(self, ...)
            local args = {...}
            
            print(string.format("🚨 Intercepted RemoteFunction: %s with %d args", remote.Name, #args))
            
            table.insert(interceptedRemotes, {
                name = remote.Name,
                args = args,
                timestamp = tick(),
                remote = remote,
                type = "RemoteFunction"
            })
            
            -- Update UI
            local interceptedTab = tabs[1].content:FindFirstChild("ScrollFrame")
            local buttonsFrame = interceptedTab and interceptedTab:FindFirstChild("ButtonsFrame")
            if interceptedTab and buttonsFrame then
                local remoteButton = Instance.new("TextButton")
                local timestamp = os.date("%H:%M:%S", math.floor(tick()))
                remoteButton.Size = UDim2.new(1, -10, 0, 60)
                remoteButton.Position = UDim2.new(0, 5, 0, #interceptedRemotes * 65)
                remoteButton.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
                remoteButton.Text = string.format("[%s] %s (%d args) [Function]", timestamp, remote.Name, #args)
                remoteButton.TextColor3 = Color3.fromRGB(255, 255, 255)
                remoteButton.TextScaled = true
                remoteButton.Font = Enum.Font.Gotham
                remoteButton.BorderSizePixel = 0
                remoteButton.Parent = buttonsFrame
                
                local buttonCorner = Instance.new("UICorner")
                buttonCorner.CornerRadius = UDim.new(0, 8)
                buttonCorner.Parent = remoteButton
                
                -- Generate Lua code
                local code = string.format("-- Intercepted: %s (RemoteFunction)\n-- Timestamp: %s\n-- Args: %d\n\n", 
                    remote.Name, timestamp, #args)
                
                for i, arg in ipairs(args) do
                    if typeof(arg) == "string" then
                        code = code .. string.format('local arg%d = "%s"\n', i, tostring(arg):gsub('"', '\\"'))
                    elseif typeof(arg) == "number" then
                        code = code .. string.format('local arg%d = %s\n', i, tostring(arg))
                    elseif typeof(arg) == "table" then
                        local success, json = pcall(function()
                            return game:GetService("HttpService"):JSONEncode(arg)
                        end)
                        code = code .. string.format('local arg%d = %s\n', i, success and json or "table")
                    else
                        code = code .. string.format('local arg%d = %s\n', i, tostring(arg))
                    end
                end
                
                code = code .. string.format('\n-- Execute with:\ngame:GetService("ReplicatedStorage"):WaitForChild("%s"):InvokeServer(%s)\n', 
                    remote.Name, table.concat({"arg1", "arg2", "arg3", "arg4", "arg5", "arg6", "arg7", "arg8"}, ", "))
                
                -- Click to load code
                remoteButton.MouseButton1Click:Connect(function()
                    switchTab(2)
                    local executeTab = tabs[2].content
                    local codeBox = executeTab:FindFirstChild("TextBox")
                    if codeBox then
                        codeBox.Text = code
                    end
                end)
                
                -- Update scroll size
                interceptedTab.CanvasSize = UDim2.new(0, 0, 0, #interceptedRemotes * 65 + 50)
            end
            
            -- Update logs
            local logsFrame = tabs[3].content
            if logsFrame then
                local logEntry = Instance.new("TextLabel")
                logEntry.Size = UDim2.new(1, -10, 0, 25)
                logEntry.Position = UDim2.new(0, 5, 0, logsFrame.AbsoluteCanvasSize.Y)
                logEntry.BackgroundTransparency = 1
                logEntry.Text = string.format("[%s] Intercepted %s with %d args [Function]", 
                    os.date("%H:%M:%S"), remote.Name, #args)
                logEntry.TextColor3 = Color3.fromRGB(0, 255, 0)
                logEntry.TextXAlignment = Enum.TextXAlignment.Left
                logEntry.TextSize = 12
                logEntry.Font = Enum.Font.Code
                logEntry.Parent = logsFrame
                
                logsFrame.CanvasSize = UDim2.new(0, 0, 0, logsFrame.AbsoluteCanvasSize.Y + 30)
            end
            
            -- Call original function
            return oldInvokeServer[remote](self, ...)
        end
    end
    
    -- 🔍 Hook all existing remotes
    for _, obj in pairs(game:GetDescendants()) do
        if obj:IsA("RemoteEvent") then
            hookRemoteEvent(obj)
        elseif obj:IsA("RemoteFunction") then
            hookRemoteFunction(obj)
        end
    end
    
    -- 🔄 Hook new remotes (dynamic)
    game.DescendantAdded:Connect(function(obj)
        if obj:IsA("RemoteEvent") then
            wait(0.1) -- Small delay for object initialization
            hookRemoteEvent(obj)
        elseif obj:IsA("RemoteFunction") then
            wait(0.1)
            hookRemoteFunction(obj)
        end
    end)
    
    print("✅ Remote Interception Engine ACTIVE! 🚨")
    print("💡 Tip: Try interacting with game objects to trigger remotes!")
end

-- 🕸️ Start Interception
spawn(function()
    wait(1) -- Wait for GUI to fully load
    interceptRemotes()
end)

-- 💬 Success Notification
local notify = Instance.new("TextLabel")
notify.Size = UDim2.new(0, 300, 0, 50)
notify.Position = UDim2.new(0.5, -150, 0, 50)
notify.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
notify.Text = "🚨 REMOTE INTERCEPTOR v2.1 FULLY ACTIVATED! 🚨"
notify.TextColor3 = Color3.fromRGB(255, 255, 255)
notify.TextScaled = true
notify.Font = Enum.Font.GothamBold
notify.Parent = screenGui

local notifyCorner = Instance.new("UICorner")
notifyCorner.CornerRadius = UDim.new(0, 8)
notifyCorner.Parent = notify

-- Animate notification
TweenService:Create(notify, TweenInfo.new(3), {Position = UDim2.new(1, 300, 0, 50)}):Play()
wait(3)
TweenService:Create(notify, TweenInfo.new(0.5), {Position = UDim2.new(1, -150, 0, 50)}):Play()
wait(0.5)
notify:Destroy()

print("💀 wino444's Remote Interceptor v2.1 - PART 2/2 Loaded! 💀")
print("🎯 Interception system online. Happy hacking! 🏴‍☠️")
